import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CON-35-agregar-comentario-tarjeta',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
